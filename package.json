{
  "name": "omp-workspace-write-guard",
  "version": "0.4.1",
  "description": "Prompt before OMP writes outside the current workspace.",
  "type": "module",
  "files": [
    "bash-targets.ts",
    "config.ts",
    "index.ts",
    "workspace-write-guard.json",
    "README.md",
    "README.zh-CN.md"
  ],
  "scripts": {
    "test": "node --test tests/workspace-write-guard.test.mjs"
  },
  "keywords": [
    "oh-my-pi",
    "omp",
    "security",
    "workspace"
  ],
  "omp": {
    "extensions": [
      "./index.ts"
    ]
  },
  "engines": {
    "node": ">=22.18"
  }
}
